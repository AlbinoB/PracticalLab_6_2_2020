<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mitwpu.practicallab_6_2_2020">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ExplicitIndentSecond"></activity>
        <activity android:name=".ExplicitIndent" />
        <activity android:name=".CheckboxActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DatePicker" />
        <activity android:name=".StudentRegisteration" />
        <activity android:name=".ImplicitIndent" />
    </application>

</manifest>